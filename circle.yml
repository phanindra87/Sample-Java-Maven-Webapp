test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - mvn surefire:test
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;## YAML Template.

